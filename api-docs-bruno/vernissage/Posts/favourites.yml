info:
  name: favourites
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/favourites"
  params:
    - name: maxId
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
