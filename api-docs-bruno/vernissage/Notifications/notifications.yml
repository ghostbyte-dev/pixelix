info:
  name: notifications
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/v1/notifications"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
