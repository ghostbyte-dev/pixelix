info:
  name: Follow Hashtag
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/v1/hashtags/:tag/follow"
  params:
    - name: tag
      value: pixelfed
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
