info:
  name: Instance
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/v1/instance"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
