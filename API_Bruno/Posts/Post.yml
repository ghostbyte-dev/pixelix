info:
  name: Post
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/statuses/:id"
  params:
    - name: id
      value: "7652713152521439863"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
