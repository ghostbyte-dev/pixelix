info:
  name: Post context
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v1/statuses/:id/context"
  params:
    - name: id
      value: "7665295534092987052"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
