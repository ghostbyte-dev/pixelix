info:
  name: Follow Requests
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/follow-requests"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
