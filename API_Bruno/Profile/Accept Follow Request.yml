info:
  name: Accept Follow Request
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/v1/follow_requests/${id}/authorize"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
