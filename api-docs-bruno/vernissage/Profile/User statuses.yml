info:
  name: User statuses
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/users/:userName/statuses"
  params:
    - name: maxId
      value: ""
      type: query
      disabled: true
    - name: userName
      value: hiebeler05
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
