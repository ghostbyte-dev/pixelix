info:
  name: LocalTimeline
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/timelines/public?onlyLocal=true"
  params:
    - name: onlyLocal
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
