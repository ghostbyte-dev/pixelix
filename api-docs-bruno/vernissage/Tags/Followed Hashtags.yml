info:
  name: Followed Hashtags
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/hashtags/followed"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
